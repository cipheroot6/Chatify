# VITE_API_URL is intentionally not set here.
# Axios uses a relative baseURL so Vite's proxy handles routing in dev,
# and Vercel rewrites handle it in production.
# See frontend/src/lib/axios.js and vite.config.js
